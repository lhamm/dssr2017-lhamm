function labeledBar(xLabels, yValues)
% first argument is a list of strings describing types or errors, but
% nothing about correct
% second argument is a list of proportions (between 0 and 1), which
% includes correct proportion, and therefore should be one longer than the
% length of possible errors

bar(Proportion)

